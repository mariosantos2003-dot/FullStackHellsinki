//Crear una funcion para que se sumen dos numeros
const sum = (sum1,sum2) => {
    console.log(sum1)
    console.log(sum2)
    return sum1 + sum2
}

//Llamamos a la funcion

const result = sum(1,2)
console.log(result) // se imprime 3